
// ---------------------------------------------------------------------
// Domain models: Book and Author records.
// ---------------------------------------------------------------------
public record Book(int Id, string Title, int AuthorId);
